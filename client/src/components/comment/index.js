import React from "react";

const Comment = ({ id }) => {
  return (
    <div className="comment">
      <h1>New comment goes here</h1>
      <textarea></textarea>
      <button>Post</button>
    </div>
  );
};

export default Comment;
